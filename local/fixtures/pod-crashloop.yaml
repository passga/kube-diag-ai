apiVersion: v1
kind: Pod
metadata:
  name: pod-crashloop
  namespace: demo
  labels:
    app.kubernetes.io/name: kube-diag-fixture
    fixture: pod-crashloop
spec:
  restartPolicy: Always
  containers:
    - name: crash
      image: busybox:1.36
      command:
        - sh
        - -c
        - echo "intentional fixture crash" && exit 1
