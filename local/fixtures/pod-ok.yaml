apiVersion: v1
kind: Pod
metadata:
  name: pod-ok
  namespace: demo
  labels:
    app.kubernetes.io/name: kube-diag-fixture
    fixture: pod-ok
spec:
  restartPolicy: Never
  containers:
    - name: ok
      image: busybox:1.36
      command:
        - sh
        - -c
        - sleep 3600
