apiVersion: v1
kind: Pod
metadata:
  name: pod-imagepullbackoff
  namespace: demo
  labels:
    app.kubernetes.io/name: kube-diag-fixture
    fixture: pod-imagepullbackoff
spec:
  restartPolicy: Never
  containers:
    - name: bad-image
      image: registry.k8s.io/kube-diag-ai/does-not-exist:missing
      imagePullPolicy: Always
      command:
        - sh
        - -c
        - sleep 3600
